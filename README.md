# SOC-Dashboard-View
 SOC Tracker is a SIEM project I built to simulate a real SOC workflow. It ingests auth and web logs, runs rule-based detections (brute force, high request rate, impossible travel), and shows alerts live in a React dashboard with charts. The backend is FastAPI with a SQL database, and there’s a log generator to create realistic traffic.
